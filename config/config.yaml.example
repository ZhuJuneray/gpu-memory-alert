telegram:
  bot_token: "YOUR_BOT_TOKEN"
  chat_id: "YOUR_CHAT_ID"

monitor:
  threshold: 20  # GPU memory threshold (percentage)
  check_interval: 60  # Check interval (seconds)
  cooldown: 300  # Minimum interval between two alerts (seconds)
